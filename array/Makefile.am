# The name "array" clashes with the C++'s <array> header. When the binary is recompiled,
# the preprocessor tires to include the binary instead of the actual header.
bin_PROGRAMS = ary
ary_SOURCES = main.cc utility.cc
